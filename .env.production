# GA4 측정 ID — **비밀이 아니다.** 페이지 소스에 그대로 실리는 값이라 감출 이유가 없고,
# 감추면 오히려 다른 기계나 CI 에서 빌드했을 때 조용히 계측 없는 사이트가 나간다.
# 그래서 저장소가 든다.
#
# 파일 이름이 `.production` 인 것이 장치다 — Next 는 `build` 에서만 이 파일을 읽는다.
# `dev` 는 못 읽으므로, 글을 고치는 동안의 새로고침은 통계에 들어가지 않는다.
NEXT_PUBLIC_GA_ID=G-9HMBYRTN4J
