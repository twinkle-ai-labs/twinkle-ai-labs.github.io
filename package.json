{
  "name": "twinkle-labs-home",
  "version": "0.1.0",
  "private": true,
  "description": "Twinkle AI Labs 소개 — 이름의 앞마당. twinklelabs.kr",
  "scripts": {
    "dev": "lsof -ti:3944 | xargs kill -9 2>/dev/null || true && next dev -p ${PORT:-3944}",
    "build": "HOME_EXPORT=1 next build && rm -rf out && mv .next-export out",
    "preview": "npm run build && npx --yes serve out -l 3945"
  },
  "dependencies": {
    "next": "^15.5.4",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.7.0"
  }
}
