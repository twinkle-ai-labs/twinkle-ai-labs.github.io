/**
 * 브랜드의 별 — `brand/symbol-*.svg` 의 그 모양.
 *
 * 이 길(path)은 화면([StarMark])이 쓰고, 파비콘·앱 아이콘은 같은 모양을 `brand` 가 굽는다.
 * 이 저장소는 제 힘으로 서야 해서 그 값을 남의 저장소에서 받아 올 수 없다 —
 * 그래서 **같은 값이 두 곳에 산다.** 두 벌은 곧 서로 달라지므로 기계가 센다:
 * 루트에서 `sh .claude/checks/star-path.sh` (모양과 보라를 함께 본다).
 *
 * 고칠 일이 생기면 `brand/build.py` 를 먼저 고치고, 그것이 찍어 주는 길을 여기 옮긴다.
 */
export const STAR_PATH =
  "M50 12 C58.36 36.624 63.376 41.64 88 50 C63.376 58.36 58.36 63.376 50 88 " +
  "C41.64 63.376 36.624 58.36 12 50 C36.624 41.64 41.64 36.624 50 12 Z";
